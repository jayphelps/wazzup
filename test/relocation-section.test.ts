import { ast, wasm } from '../';
import { assertModule } from './utils/assertModule';

describe('Relocation Section(s)', () => {
  let module;

  beforeEach(() => {
    module = new ast.Module();
  });

  test('does stuff', () => {
    
  });
});
